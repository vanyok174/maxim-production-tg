# Общий префикс URL (как в nginx). В client при сборке прод — скопируй то же в VITE_BASE_PATH.
PUBLIC_BASE_PATH=/mxprod/

# client/.env (для vite build)
# VITE_BASE_PATH=/mxprod/

PORT=3847
SQLITE_PATH=./data/app.db

BOT_TOKEN=
ADMIN_TELEGRAM_IDS=458454663

# Локальная отладка UI без Telegram (НЕ на проде)
# DEV_SKIP_AUTH=1

WB_API_TOKEN=
WB_SALES_LOOKBACK_DAYS=21
